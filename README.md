# js-closure-notification
A lightweight custom notification (toast) system built using JavaScript closures and Tailwind CSS. This project demonstrates how closures can be used to create reusable, configurable UI components.
